# Product Design QA：移动现场端首版

## 验证范围

- 路由：`/v2/field/mobile`
- 视口：390 × 844
- 状态：3 条现场任务、1 条待同步记录、1 条逾期任务
- 对照截图：`smart-bamboo-mobile-before.png` 与 `smart-bamboo-mobile-after.png`

## 结果

| 检查项 | 结果 | 说明 |
|---|---|---|
| 独立移动界面 | 通过 | 后台桌面顶栏、侧栏和“前端大屏”入口不再出现在现场端。 |
| 信息层级 | 通过 | 首屏先显示今日风险、待同步和本机任务，再进入任务分类与明细。 |
| 触控可用性 | 通过 | 返回、更新、同步、标签、任务卡和底部导航均满足手机触控尺寸。 |
| 离线状态表达 | 通过 | 在线/离线、离线包时间、待同步数量和本机安全暂存均有明确提示。 |
| 核心交互 | 通过 | 安全任务筛选、任务详情打开/关闭、底部导航均完成自动点击验证。 |
| 响应式布局 | 通过 | 390px 小屏无横向溢出，任务标题和林班编号保持可读。 |
| 控制台错误 | 通过 | 自动交互过程中无 `pageerror` 或 console error。 |
| 数据真实性 | 通过 | 页面继续读取真实离线包、任务、轨迹、凭证和同步接口；未引入演示统计。 |

## 设计取舍

- 不在现场首屏加载 Cesium、COPC 或大幅 GeoTIFF，避免弱网与低端设备的内存压力。
- 同步按钮在内容区和底部导航均可达，属于现场高频关键动作，保留双入口。
- 原生 Android/iOS 不复制业务规则，通过版本化桥接协议补齐设备能力。

## 后续 QA

- Android 真机验证相机、后台定位、弱网恢复、500MB 视频续传和系统返回。
- iOS 原型验证安全区、键盘、相册权限、后台定位及 App Store 隐私清单。
- 在 360×800、390×844、430×932 和 iPad 视口做回归截图。
