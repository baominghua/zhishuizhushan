limit_req_zone $binary_remote_addr zone=v2_login_per_ip:10m rate=5r/m;

gzip on;
gzip_vary on;
gzip_proxied any;
gzip_comp_level 6;
gzip_min_length 1024;
gzip_types text/plain text/css text/javascript application/javascript application/json application/geo+json application/xml image/svg+xml application/vnd.mapbox-vector-tile;

server {
    listen 443 ssl default_server;
    server_name _;
    server_tokens off;
    absolute_redirect off;

    ssl_certificate /etc/nginx/tls/fullchain.pem;
    ssl_certificate_key /etc/nginx/tls/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_session_cache shared:V2TLS:10m;
    ssl_session_timeout 1d;

    client_max_body_size 2g;
    proxy_connect_timeout 60s;
    proxy_read_timeout 3600s;
    proxy_send_timeout 3600s;
    limit_req_status 429;

    location = / {
        return 302 /v2/workspace;
    }

    location = /admin-login.html {
        if ($arg_returnTo = "") {
            return 302 /admin-login.html?returnTo=/v2/workspace;
        }
        proxy_pass http://app-v2-secure:8010;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }

    location = /api/auth/login {
        client_max_body_size 16k;
        limit_req zone=v2_login_per_ip burst=5 nodelay;
        proxy_pass http://app-v2-secure:8010;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }

    location /api/ {
        proxy_pass http://app-v2-secure:8010;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }

    location /v2/ {
        proxy_pass http://app-v2-secure:8010;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }

    location = /admin.css {
        proxy_pass http://app-v2-secure:8010;
    }

    location = /admin-login.js {
        proxy_pass http://app-v2-secure:8010;
    }

    location / {
        return 404;
    }
}
